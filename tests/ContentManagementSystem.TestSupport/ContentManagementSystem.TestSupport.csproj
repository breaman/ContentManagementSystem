<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\ContentManagementSystem.Data\ContentManagementSystem.Data.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- The image fixtures are generated rather than checked in, so the helper library that owns
         them encodes them for real. -->
    <PackageReference Include="SkiaSharp" />
    <PackageReference Include="Testcontainers.MsSql" />
    <PackageReference Include="TUnit.Core" />
  </ItemGroup>

</Project>
