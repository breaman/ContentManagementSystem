<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\ContentManagementSystem.Core\ContentManagementSystem.Core.csproj" />
    <ProjectReference Include="..\..\src\ContentManagementSystem.Rendering\ContentManagementSystem.Rendering.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- bUnit lives here rather than in a project of its own: rendering unit tests exercise the
         same field types and payload models as the rest of the Core suite. -->
    <PackageReference Include="bunit" />
    <PackageReference Include="NSubstitute" />
  </ItemGroup>

</Project>
