@inherits CmsFieldRendererBase
@* Renders nothing, which is what spec section 15.3 asks for when reusable content cannot be
   resolved — and until P4 builds the item store, none of it can be. The cache tag is added anyway;
   see the code-behind. *@
