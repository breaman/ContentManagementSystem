@inherits CmsFieldRendererBase
@* A list, because that is what it is — the P8 tag pages will hang links off these same items. *@
@if (Tags.Count > 0)
{
    <ul class="cms-tags">
        @foreach (var tag in Tags)
        {
            <li class="cms-tag">@tag</li>
        }
    </ul>
}
