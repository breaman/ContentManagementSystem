@namespace ContentManagementSystem.Client.Components.Admin.Shortcuts

@* Renders nothing. The listener lives on the document, because a shortcut that only worked while
   focus was inside one div would be a shortcut an editor discovers is unreliable (task P6-23). *@
